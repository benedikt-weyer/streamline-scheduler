apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: planera-staging
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: planera-config
  namespace: planera-staging
data:
  POSTGRES_USER: planera
  POSTGRES_DB: planera_db
  RUST_LOG: debug
---
apiVersion: v1
kind: Secret
metadata:
  name: planera-secrets
  namespace: planera-staging
type: Opaque
stringData:
  POSTGRES_PASSWORD: changeme-staging
  DATABASE_URL: postgresql://planera:changeme-staging@postgres:5432/planera_db
  JWT_SECRET: changeme-staging-jwt-secret-at-least-32-chars
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: planera-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: planera-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: planera-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: planera-config
              key: POSTGRES_DB
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - planera
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - planera
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: planera-staging
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None

