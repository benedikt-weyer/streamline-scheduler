apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: plandera-production
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: plandera-config
  namespace: plandera-production
data:
  POSTGRES_USER: plandera
  POSTGRES_DB: plandera_db
  RUST_LOG: info
---
apiVersion: v1
kind: Secret
metadata:
  name: plandera-secrets
  namespace: plandera-production
type: Opaque
stringData:
  POSTGRES_PASSWORD: changeme-production-REPLACE-WITH-STRONG-PASSWORD
  DATABASE_URL: postgresql://plandera:changeme-production-REPLACE-WITH-STRONG-PASSWORD@postgres:5432/plandera_db
  JWT_SECRET: changeme-production-jwt-secret-REPLACE-WITH-STRONG-SECRET-at-least-32-chars
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: plandera-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: plandera-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: plandera-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: plandera-config
              key: POSTGRES_DB
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - plandera
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - plandera
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: plandera-production
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None

