name: Deploy to Production

on:
  workflow_dispatch:
  workflow_call:

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE_NAME: ${{ github.repository_owner }}/plandera-backend
  FRONTEND_IMAGE_NAME: ${{ github.repository_owner }}/plandera-frontend

jobs:
  deploy-to-production:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://app.plandera.app
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure kubectl
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Create namespace if not exists
        run: |
          kubectl apply -f k8s/production/namespace.yaml

      - name: Create image pull secret
        run: |
          kubectl create secret docker-registry ghcr-secret \
            --docker-server=${{ env.REGISTRY }} \
            --docker-username=${{ github.actor }} \
            --docker-password=${{ secrets.GITHUB_TOKEN }} \
            --namespace=plandera-production \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy to production
        run: |
          kubectl apply -f k8s/production/postgres.yaml
          kubectl apply -f k8s/production/backend.yaml
          kubectl apply -f k8s/production/frontend.yaml
          kubectl apply -f k8s/production/ingress.yaml

      - name: Wait for backend deployment
        run: |
          kubectl rollout status deployment/backend -n plandera-production --timeout=5m

      - name: Wait for frontend deployment
        run: |
          kubectl rollout status deployment/frontend -n plandera-production --timeout=5m

      - name: Verify deployment
        run: |
          kubectl get pods -n plandera-production
          kubectl get services -n plandera-production
          kubectl get ingress -n plandera-production

      - name: Run smoke tests
        run: |
          echo "Waiting 30 seconds for services to stabilize..."
          sleep 30
          
          echo "Testing backend health endpoint..."
          kubectl run curl-test --image=curlimages/curl:latest --rm -i --restart=Never --namespace=plandera-production -- \
            curl -f http://backend.plandera-production.svc.cluster.local:8080/health || exit 1
          
          echo "All smoke tests passed!"
