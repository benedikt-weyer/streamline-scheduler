name: Deploy to Staging

on:
  workflow_dispatch:
  workflow_call:

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE_NAME: ${{ github.repository_owner }}/planera-backend
  FRONTEND_IMAGE_NAME: ${{ github.repository_owner }}/planera-frontend

jobs:
  deploy-to-staging:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure kubectl
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Create namespace if not exists
        run: |
          kubectl apply -f k8s/staging/namespace.yaml

      - name: Create image pull secret
        run: |
          kubectl create secret docker-registry ghcr-secret \
            --docker-server=${{ env.REGISTRY }} \
            --docker-username=${{ github.actor }} \
            --docker-password=${{ secrets.GITHUB_TOKEN }} \
            --namespace=planera-staging \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy to staging
        run: |
          kubectl apply -f k8s/staging/postgres.yaml
          kubectl apply -f k8s/staging/backend.yaml
          kubectl apply -f k8s/staging/frontend.yaml
          kubectl apply -f k8s/staging/ingress.yaml

      - name: Wait for backend deployment
        run: |
          kubectl rollout status deployment/backend -n planera-staging --timeout=5m

      - name: Wait for frontend deployment
        run: |
          kubectl rollout status deployment/frontend -n planera-staging --timeout=5m

      - name: Verify deployment
        run: |
          kubectl get pods -n planera-staging
          kubectl get services -n planera-staging
          kubectl get ingress -n planera-staging
