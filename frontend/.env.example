# Backend API Configuration
# These are runtime env vars (no NEXT_PUBLIC_ prefix) served via /api/config.
# Set them in your container/deployment — they are NOT baked in at build time.
PUBLIC_BACKEND_HTTP_URL=http://localhost:3001
PUBLIC_BACKEND_WS_URL=ws://localhost:3001

NODE_ENV=development
